import { Meta, Story } from "@storybook/addon-docs";

import { SitesListItem } from ".";

<Meta
    title={"Mobile/Sites/사이트 목록-리스트 아이템"}
    component={SitesListItem}
/>

# SitesListItem

사이트 목록 리스트 아이템

export const Template = (args) => <SitesListItem {...args} />;

<Canvas>
    <Story
        name="Default"
        args={{
            model: {
                id: 1,
                title: "편백 경로당",
                address: "서울",
                createdAt: "2021.01.01",
            },
            onClick: () => alert(`clicked ${1}`),
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

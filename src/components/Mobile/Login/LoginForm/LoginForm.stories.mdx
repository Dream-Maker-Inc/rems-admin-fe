import { Meta, Story } from "@storybook/addon-docs";

import { LoginForm } from ".";
import { useState } from "react";
import { useArgs } from "@storybook/client-api";

<Meta title={"Mobile/Login/로그인 폼"} component={LoginForm} />

# LoginForm

로그인 페이지 폼

export const Template = (args) => {
    return (
        <LoginForm
            handleIdChange={() => {}}
            handlePwChange={() => {}}
            handleRememberChange={() => {}}
            handleLoginSubmit={() => alert("login ok")}
            {...args}
        />
    );
};

<Canvas>
    <Story
        name="Default"
        args={{
            idValue: "",
            pwValue: "",
            rememberMe: false,
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable of={LoginForm} />

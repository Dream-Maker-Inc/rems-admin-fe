import { Meta, Story } from "@storybook/addon-docs";

import { SectionTemplate } from ".";

<Meta title={"Mobile/Common/섹션 템플릿"} component={SectionTemplate} />

# SectionTemplate

섹션 템플릿

export const Template = (args) => <SectionTemplate {...args} />;

<Canvas>
    <Story
        name="Default"
        args={{
            title: "에어컨(냉방)",
            children: "contents",
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

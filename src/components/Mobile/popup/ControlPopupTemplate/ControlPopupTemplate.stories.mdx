import { Meta, Story } from "@storybook/addon-docs";

import { ControlPopupTemplate } from ".";

<Meta title={"Control/[Popup] 설비 제어"} component={ControlPopupTemplate} />

# ControlPopupTemplate

설비 제어

export const Template = (args) => <ControlPopupTemplate {...args} />;

<Canvas>
  <Story name="Default" args={{ children: "콘텐츠 크기에 따라 height 변동" }}>
    {Template.bind({})}
  </Story>
</Canvas>

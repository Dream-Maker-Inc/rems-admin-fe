import { Meta, Story } from "@storybook/addon-docs";

import { GearControlView } from ".";

<Meta
    title={"Mobile/Gear-Control/[Page] 설비 제어 뷰"}
    component={GearControlView}
/>

# GearControlView

설비 제어 뷰

export const Template = (args) => <GearControlView {...args} />;

<Canvas>
    <Story name="Default">{Template.bind({})}</Story>
</Canvas>

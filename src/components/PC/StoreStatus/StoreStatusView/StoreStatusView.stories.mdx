import { Meta, Story } from "@storybook/addon-docs";

import { StoreStatusView } from ".";

<Meta
    title={"PC/StoreStatus/[Page] 점포별 현황 뷰"}
    component={StoreStatusView}
/>

# StoreStatusView

점포별 현황 뷰

export const Template = (args) => <StoreStatusView {...args} />;

<Canvas>
    <Story name="Default">{Template.bind({})}</Story>
</Canvas>

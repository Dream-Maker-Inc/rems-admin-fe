import { Meta, Story } from "@storybook/addon-docs";

import { ItemCard } from ".";

<Meta title={"PC/Group-Select/그룹 선택 아이템 카드"} component={ItemCard} />

# ItemCard

그룹 선택 아이템 카드

export const Template = (args) => <ItemCard {...args} />;

<Canvas>
  <Story
    name="Default"
    args={{
      logoImageUrl: `${process.env.PUBLIC_URL}/assets/images/logo_cu.png`,
      bgImageUrl: `${process.env.PUBLIC_URL}/assets/images/bg_store.png`,
      imageAlt: "경로당",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

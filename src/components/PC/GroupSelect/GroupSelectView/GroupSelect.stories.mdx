import { Meta, Story } from "@storybook/addon-docs";

import { GroupSelectView } from ".";

<Meta
  title={"PC/Group-Select/[Page] 그룹 선택 뷰"}
  component={GroupSelectView}
/>

# GroupSelectView

그룹 선택 뷰

export const Template = (args) => <GroupSelectView {...args} />;

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

import { Meta, Story } from "@storybook/addon-docs";

import { Map } from ".";

<Meta title={"PC/Dashboard/[Map] 지역별 현황"} component={Map} />

# Map

지역별 현황 맵

export const Template = (args) => <Map {...args} />;

<Canvas>
    <Story
        name="Default"
        args={{
            leftItems: [1, 2, 3, 4, 5],
            rightItems: [1, 2, 3, 4, 5],
            legends: [
                {
                    color: "skyblue",
                    label: "가동 중",
                },
                {
                    color: "lightgrey",
                    label: "미가동",
                },
            ],
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

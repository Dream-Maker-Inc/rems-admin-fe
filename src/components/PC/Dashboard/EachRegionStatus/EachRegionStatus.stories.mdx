import { Meta, Story } from "@storybook/addon-docs";

import { EachRegionStatus } from ".";

<Meta title={"PC/Dashboard/지역별 현황"} component={EachRegionStatus} />

# EachRegionStatus

지역별 현황

export const Template = (args) => <EachRegionStatus {...args} />;

<Canvas>
    <Story
        name="Default"
        args={{ leftItems: [1, 2, 3, 4, 5], rightItems: [1, 2, 3, 4, 5] }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

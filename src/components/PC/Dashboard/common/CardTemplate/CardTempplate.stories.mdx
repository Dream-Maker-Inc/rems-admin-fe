import { Meta, Story } from "@storybook/addon-docs";

import { CardTempplate } from ".";

<Meta title={"PC/Dashboard/Common/카드 템플릿"} component={CardTempplate} />

# CardTempplate

카드 템플릿

export const Template = (args) => <CardTempplate {...args} />;

<Canvas>
    <Story name="Default" args={{ title: "Title", children: "contents" }}>
        {Template.bind({})}
    </Story>
</Canvas>

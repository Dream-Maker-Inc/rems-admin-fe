import { Meta, Story } from "@storybook/addon-docs";

import { CardTemplate } from ".";

<Meta title={"PC/Dashboard/Common/카드 템플릿"} component={CardTemplate} />

# CardTemplate

카드 템플릿

export const Template = (args) => <CardTemplate {...args} />;

<Canvas>
    <Story name="Default" args={{ title: "Title", children: "contents" }}>
        {Template.bind({})}
    </Story>
</Canvas>
